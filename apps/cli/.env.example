# Copy to `.env` (in this directory or any parent — auto-loaded at startup)
# and fill in the values you actually use. Explicit shell exports always win
# over .env, so you can override any single value per-invocation.
#
# Tip: `truthmarket dev up` writes the foundry/anvil values for you.

# ----- chain -----
# foundry | baseSepolia | sepolia
TM_CHAIN=sepolia
TM_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
TM_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
TM_REGISTRY_ADDRESS=0xa50B3e0Ca53f28Fb8BD0a3e0DdbFbE7fE36047E5
TM_STAKE_TOKEN=0x543dDb01Ba47acB11de34891cD86B675F04840db
# Optional per-clone default. When unset, registry create/agent create use the creator wallet.
TM_JURY_COMMITTER=0x574F91bd4d8e83F84B62c3Ca75d24684813237Cc
# Optional: verified-fetch defaults to a public Swarm gateway when unset.
# TM_SWARM_GATEWAY_URL=
# Optional: claim/rules publishing defaults to the public Swarm testnet upload gateway.
# TM_SWARM_BEE_API_URL=
# TM_SWARM_POSTAGE_BATCH_ID=

# ----- wallet -----
# Anvil deterministic deployer key — replace for any non-local network.
# DO NOT commit a real key. Use the encrypted keystore instead:
#   truthmarket wallet init
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Set if you want non-interactive runs against an encrypted keystore.
# TM_KEYSTORE_PASSPHRASE=

# Vault is the per-market (vote, nonce, stake) blob; needed for reveal.
# TM_VAULT_PASSPHRASE=

# ----- paths -----
# Override `~/.truthmarket` if you want config/keystore/vault elsewhere.
# TM_HOME=
# TM_POLICY_FILE=
